language: minimal

before_install:
  - wget https://github.com/vlang/v/archive/master.zip
  - unzip master.zip
  - cd v-master
  - make
  - cd ..
  - alias v=’v-master/v’
  - chmod +x compile.sh

script:
  - ./compile.sh
