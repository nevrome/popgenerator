language: minimal

before_install:
  - wget https://github.com/vlang/v/archive/master.zip
  - unzip master.zip
  - cd v-master
  - make
  - cd ..
  - chmod +x compile.sh

script:
  - alias v=’v-master/v’
  - ./compile.sh
